package tp.pr1.mv.command;
/**
 * 
 * ommandParser: Clase que se encarga de generar los comandos. Contiene el método
estático public static CommandInterpreter parseCommand(String line), que procesa el
string de entrada y o bien devuelve un objeto de la clase CommandInterpreter o null
en caso de que la línea de entrada no se corresponda con ningún comando.
 * @author NASAIII
 *
 */
public class CommandParser {
	 public static CommandInterpreter parseCommand(String line) {
		return null;
	}
}
